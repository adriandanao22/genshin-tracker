name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  # Required by the branch ruleset — must stay green to merge.
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # Build first: `next build` type-checks AND generates next-env.d.ts +
      # .next/types, which the standalone tsc step below needs. (next-env.d.ts
      # is gitignored, so on a clean checkout it doesn't exist yet.)
      - name: Build
        run: pnpm run build
        env:
          # Not used at build time (routes read env at request time); a dummy
          # keeps any accidental build-time access from failing CI.
          HOYOLAB_SESSION_SECRET: ci-build-placeholder
      - name: Typecheck
        run: pnpm exec tsc --noEmit
      - name: Lint (non-blocking)
        run: pnpm run lint
        continue-on-error: true
